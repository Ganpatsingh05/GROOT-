# Install GSC launcher on Windows

1. Copy `bin/gsc.bat` to a folder, e.g. `C:\gsc-cli\`
2. Add `C:\gsc-cli\` to your PATH (Environment Variables -> Path -> New)
3. Open a new Command Prompt and run: `gsc --help`

Note: Adjust the path inside gsc.bat if you moved main.py.
